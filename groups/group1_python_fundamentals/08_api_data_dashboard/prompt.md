build a python script that fetches data from https://jsonplaceholder.typicode.com/posts and https://jsonplaceholder.typicode.com/users, then generates an HTML dashboard showing: a bar chart of posts per user (using their name not id), and a table listing all users with their post count. use the requests library for fetching and matplotlib or plotly for the chart. save the output as dashboard.html with the chart embedded as an image or interactive plot.
